//! Generated by waldur-cli-generator from `commands.toml`. Do not edit by hand;
//! see that repo's README for how to regenerate.
#![allow(clippy::too_many_arguments)]
use waldur_client::HttpClient;
const COLUMNS: &[&str; 3usize] = &["uuid", "project_name", "is_pending"];
///Project permission reviews (read-only)
#[derive(clap::Subcommand, Debug)]
pub enum ProjectPermissionsReviewCommand {
    ///List project permission reviews (read-only)
    List(ProjectPermissionsReviewListArgs),
    ///Get project permission reviews (read-only)
    Get(ProjectPermissionsReviewGetArgs),
}
#[derive(clap::Args, Debug)]
pub struct ProjectPermissionsReviewListArgs {
    #[arg(long)]
    pub closed: Option<String>,
    #[arg(long)]
    pub is_pending: Option<bool>,
    #[arg(long)]
    pub project_uuid: Option<String>,
    #[arg(long)]
    pub reviewer_uuid: Option<String>,
    /// Stop after this many items (across however many pages that
    /// takes), instead of fetching the complete result
    #[arg(long)]
    pub limit: Option<i64>,
}
#[derive(clap::Args, Debug)]
pub struct ProjectPermissionsReviewGetArgs {
    pub uuid: String,
}
pub async fn run(
    client: &HttpClient,
    base_url: &str,
    token: Option<&str>,
    command: ProjectPermissionsReviewCommand,
    format: crate::output::OutputFormat,
) -> anyhow::Result<()> {
    match command {
        ProjectPermissionsReviewCommand::List(args) => {
            let mut query_params: Vec<(String, String)> = Vec::new();
            if let Some(v) = &args.closed {
                query_params.push(("closed".to_string(), v.clone()));
            }
            if let Some(v) = args.is_pending {
                query_params.push(("is_pending".to_string(), v.to_string()));
            }
            if let Some(v) = &args.project_uuid {
                query_params.push(("project_uuid".to_string(), v.clone()));
            }
            if let Some(v) = &args.reviewer_uuid {
                query_params.push(("reviewer_uuid".to_string(), v.clone()));
            }
            let result = crate::pagination::fetch_all(
                    base_url,
                    token,
                    "/api/project-permissions-reviews/",
                    &query_params,
                    args.limit,
                )
                .await?;
            crate::output::print_result(&result, COLUMNS, format)?;
        }
        ProjectPermissionsReviewCommand::Get(args) => {
            let result = client
                .project_permissions_reviews_retrieve(args.uuid.as_str())
                .await?;
            crate::output::print_result(&result, COLUMNS, format)?;
        }
    }
    Ok(())
}
