//! Generated by waldur-cli-generator from `commands.toml`. Do not edit by hand;
//! see that repo's README for how to regenerate.
#![allow(clippy::too_many_arguments)]
const COLUMNS: &[&str; 4usize] = &["uuid", "name", "offering_name", "state"];
const FILTER_SPEC: &[(&str, crate::filter::FilterKind)] = &[
    ("backend_id", crate::filter::FilterKind::Str),
    ("category_uuid", crate::filter::FilterKind::Str),
    ("created", crate::filter::FilterKind::Str),
    ("created_before", crate::filter::FilterKind::Str),
    ("customer", crate::filter::FilterKind::Str),
    ("customer_uuid", crate::filter::FilterKind::Str),
    ("downscaled", crate::filter::FilterKind::Bool),
    ("flavor_name", crate::filter::FilterKind::Str),
    ("has_terminate_date", crate::filter::FilterKind::Bool),
    ("image_name", crate::filter::FilterKind::Str),
    ("is_attached", crate::filter::FilterKind::Bool),
    ("lexis_links_supported", crate::filter::FilterKind::Bool),
    ("limit_based", crate::filter::FilterKind::Bool),
    ("modified", crate::filter::FilterKind::Str),
    ("modified_before", crate::filter::FilterKind::Str),
    ("name", crate::filter::FilterKind::Str),
    ("name_exact", crate::filter::FilterKind::Str),
    ("o", crate::filter::FilterKind::Str),
    ("offering", crate::filter::FilterKind::Str),
    ("offering_billable", crate::filter::FilterKind::Bool),
    ("offering_shared", crate::filter::FilterKind::Bool),
    ("offering_slug", crate::filter::FilterKind::Str),
    ("offering_type", crate::filter::FilterKind::Str),
    ("offering_uuid", crate::filter::FilterKind::Str),
    ("only_limit_based", crate::filter::FilterKind::Bool),
    ("only_usage_based", crate::filter::FilterKind::Bool),
    ("order_state", crate::filter::FilterKind::Str),
    ("parent_offering_uuid", crate::filter::FilterKind::Str),
    ("paused", crate::filter::FilterKind::Bool),
    ("plan_uuid", crate::filter::FilterKind::Str),
    ("project_name", crate::filter::FilterKind::Str),
    ("project_uuid", crate::filter::FilterKind::Str),
    ("provider_uuid", crate::filter::FilterKind::Str),
    ("query", crate::filter::FilterKind::Str),
    ("resource_attributes", crate::filter::FilterKind::Str),
    ("restrict_member_access", crate::filter::FilterKind::Bool),
    ("runtime_state", crate::filter::FilterKind::Str),
    ("scope", crate::filter::FilterKind::Str),
    ("service_manager_uuid", crate::filter::FilterKind::Str),
    ("slug", crate::filter::FilterKind::Str),
    ("state", crate::filter::FilterKind::Str),
    ("usage_based", crate::filter::FilterKind::Bool),
    ("visible_to_providers", crate::filter::FilterKind::Bool),
    ("visible_to_username", crate::filter::FilterKind::Str),
];
const PROVISION_SKELETON: &str = "{\n  \"accepting_terms_of_service\": true,\n  \"attributes\": {\n    \"description\": null,\n    \"name\": null\n  },\n  \"callback_url\": null,\n  \"limits\": null,\n  \"offering\": \"\",\n  \"plan\": null,\n  \"project\": \"\",\n  \"request_comment\": null,\n  \"slug\": null,\n  \"start_date\": null,\n  \"type\": null\n}";
///Marketplace resources (provision/terminate any offering)
#[derive(clap::Subcommand, Debug)]
pub enum ResourceCommand {
    ///List marketplace resources (provision/terminate any offering)
    List(ResourceListArgs),
    ///Get marketplace resources (provision/terminate any offering)
    Get(ResourceGetArgs),
    ///Provision marketplace resources (provision/terminate any offering) via a marketplace order
    Provision(ResourceProvisionArgs),
    ///Terminate marketplace resources (provision/terminate any offering) via a marketplace order
    Terminate(ResourceTerminateArgs),
}
#[derive(clap::Args, Debug)]
pub struct ResourceListArgs {
    /// Filter results server-side, KEY=VALUE (repeatable). See
    /// --help's error on an unknown key for the valid keys.
    #[arg(long = "filter", value_name = "KEY=VALUE")]
    pub filter: Vec<String>,
    /// Reshape/narrow the already-fetched result with a JMESPath
    /// expression (https://jmespath.org), client-side -- e.g.
    /// [].name or [?blocked==`true`]. Applied after fetching,
    /// before rendering in --format. (Named distinctly from
    /// --filter's own `query` key, several resources' own
    /// full-text search field.)
    #[arg(long)]
    pub jmespath: Option<String>,
    /// Stop after this many items (across however many pages that
    /// takes), instead of fetching the complete result
    #[arg(long)]
    pub limit: Option<i64>,
    ///Only fetch these fields from the server (comma-separated), instead of the complete object -- avoids over-fetching. Table output always does this already (using its own display columns); for json/toon/tsv, which fetch the complete object by default, this narrows what they get too.
    #[arg(
        long,
        value_delimiter = ',',
        value_parser = clap::builder::PossibleValuesParser::new(
            ["attributes",
            "available_actions",
            "backend_id",
            "backend_metadata",
            "can_terminate",
            "category_icon",
            "category_title",
            "category_uuid",
            "created",
            "creation_order",
            "current_usages",
            "customer_name",
            "customer_slug",
            "customer_uuid",
            "description",
            "downscaled",
            "effective_id",
            "end_date",
            "end_date_requested_by",
            "end_date_updated_at",
            "endpoints",
            "error_message",
            "error_traceback",
            "is_limit_based",
            "is_usage_based",
            "last_sync",
            "limit_usage",
            "limits",
            "modified",
            "name",
            "offering",
            "offering_backend_id",
            "offering_billable",
            "offering_components",
            "offering_description",
            "offering_image",
            "offering_name",
            "offering_plugin_options",
            "offering_shared",
            "offering_slug",
            "offering_state",
            "offering_thumbnail",
            "offering_type",
            "offering_uuid",
            "options",
            "order_in_progress",
            "parent_name",
            "parent_offering_name",
            "parent_offering_slug",
            "parent_offering_uuid",
            "parent_uuid",
            "paused",
            "plan",
            "plan_description",
            "plan_name",
            "plan_unit",
            "plan_uuid",
            "project",
            "project_description",
            "project_effective_end_date",
            "project_end_date",
            "project_end_date_requested_by",
            "project_is_in_grace_period",
            "project_name",
            "project_slug",
            "project_uuid",
            "provider_description",
            "provider_name",
            "provider_slug",
            "provider_uuid",
            "renewal_date",
            "report",
            "resource_type",
            "resource_uuid",
            "restrict_member_access",
            "scope",
            "service_settings_uuid",
            "slug",
            "state",
            "url",
            "user_requires_reconsent",
            "username",
            "uuid"]
        ),
    )]
    pub fields: Option<Vec<String>>,
}
#[derive(clap::Args, Debug)]
pub struct ResourceGetArgs {
    pub uuid: String,
}
#[derive(clap::Args, Debug)]
#[command(
    group(
        clap::ArgGroup::new(
            "resource_provision_body"
        ).required(true).args(["request", "request_file", "generate_skeleton"])
    )
)]
pub struct ResourceProvisionArgs {
    /// The marketplace order body as inline JSON. Use
    /// --generate-skeleton for a template (offering/project plus
    /// this resource's typed attributes), or --request-file to
    /// read it from a file.
    #[arg(long)]
    pub request: Option<String>,
    /// Read the order body from a JSON or YAML file.
    #[arg(long, value_name = "PATH")]
    pub request_file: Option<std::path::PathBuf>,
    /// Print a fillable order template and exit, instead of
    /// submitting (json or yaml; default json).
    #[arg(
        long,
        value_enum,
        num_args = 0..= 1,
        default_missing_value = "json",
        value_name = "FORMAT"
    )]
    pub generate_skeleton: Option<crate::request::SkeletonFormat>,
    /// Submit the order and return immediately, without polling
    /// it to completion.
    #[arg(long)]
    pub no_wait: bool,
    /// Seconds to wait for the order to reach a terminal state
    /// before giving up (ignored with --no-wait).
    #[arg(long, default_value_t = 600)]
    pub timeout: u64,
}
#[derive(clap::Args, Debug)]
pub struct ResourceTerminateArgs {
    /// The marketplace resource UUID (a resource's
    /// `marketplace_resource_uuid` field, from get/list) -- not
    /// the plugin resource's own UUID.
    pub uuid: String,
    /// Optional termination attributes as inline JSON, e.g.
    /// '{"delete_volumes": true}'.
    #[arg(long)]
    pub request: Option<String>,
    /// Submit the termination and return immediately, without
    /// polling the order to completion.
    #[arg(long)]
    pub no_wait: bool,
    /// Seconds to wait for the termination order before giving
    /// up (ignored with --no-wait).
    #[arg(long, default_value_t = 600)]
    pub timeout: u64,
}
pub async fn run(
    _client: &waldur_client::HttpClient,
    base_url: &str,
    token: Option<&str>,
    project: Option<&str>,
    dry_run: bool,
    command: ResourceCommand,
    format: crate::output::OutputFormat,
) -> anyhow::Result<()> {
    match command {
        ResourceCommand::List(args) => {
            let mut query_params: Vec<(String, String)> = crate::filter::parse_filters(
                &args.filter,
                FILTER_SPEC,
            )?;
            if let Some(project) = project {
                if !query_params.iter().any(|(k, _)| k == "project_uuid") {
                    query_params.push(("project_uuid".to_string(), project.to_string()));
                }
            }
            match &args.fields {
                Some(fields) => {
                    for f in fields {
                        query_params.push(("field".to_string(), f.clone()));
                    }
                }
                None => {
                    if matches!(format, crate ::output::OutputFormat::Table) {
                        for f in COLUMNS {
                            query_params.push(("field".to_string(), (*f).to_string()));
                        }
                    }
                }
            }
            if matches!(format, crate ::output::OutputFormat::Ndjson)
                && args.jmespath.is_none()
            {
                crate::pagination::fetch_all_streaming(
                        base_url,
                        token,
                        "/api/marketplace-resources/",
                        &query_params,
                        args.limit,
                        |item| crate::output::print_ndjson_line(&item),
                    )
                    .await?;
            } else {
                let result = crate::pagination::fetch_all(
                        base_url,
                        token,
                        "/api/marketplace-resources/",
                        &query_params,
                        args.limit,
                    )
                    .await?;
                let display_columns: Vec<&str> = match &args.fields {
                    Some(fields) => fields.iter().map(String::as_str).collect(),
                    None => COLUMNS.to_vec(),
                };
                let result: serde_json::Value = serde_json::Value::Array(result);
                let result = match &args.jmespath {
                    Some(expr) => crate::query::apply(result, expr)?,
                    None => result,
                };
                crate::output::print_result(&result, &display_columns, format)?;
            }
        }
        ResourceCommand::Get(args) => {
            let path = format!("{}{}{}", "/api/marketplace-resources/", args.uuid, "/");
            let result = crate::http::call_one(
                    base_url,
                    token,
                    reqwest::Method::GET,
                    &path,
                    None,
                )
                .await?;
            crate::output::print_result(&result, COLUMNS, format)?;
        }
        ResourceCommand::Provision(args) => {
            if let Some(fmt) = args.generate_skeleton {
                crate::request::print_skeleton(PROVISION_SKELETON, fmt)?;
                return Ok(());
            }
            let body = crate::request::load_body(
                args.request.as_deref(),
                args.request_file.as_deref(),
            )?;
            crate::order::provision(
                    base_url,
                    token,
                    &body,
                    project,
                    dry_run,
                    !args.no_wait,
                    args.timeout,
                    format,
                )
                .await?;
        }
        ResourceCommand::Terminate(args) => {
            crate::order::terminate(
                    base_url,
                    token,
                    &args.uuid,
                    args.request.as_deref(),
                    dry_run,
                    !args.no_wait,
                    args.timeout,
                    format,
                )
                .await?;
        }
    }
    Ok(())
}
